OverTheWire

Online based Linux War CTF

OverTheWire Commands

    man (to know the purpose of the command) press q to quit.

    help command (help cd)
        USERNAMES are somegame0, somegame1, ...
        Most LEVELS are stored in /somegame/.
        PASSWORDS for each level are stored in /etc/somegame_pass/.

New Commands: ls -alps find . -type f | xargs file find . -type f size 1033c ! -executable

login id : ssh bandit_@bandit.labs.overthewire.org
